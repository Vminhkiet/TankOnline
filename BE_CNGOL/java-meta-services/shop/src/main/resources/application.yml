server:
  port: 8088
  max-http-header-size: 32768

spring:
  application:
    name: shop-service
  datasource:
    url: jdbc:mysql://localhost:3307/shoptank_db?createDatabaseIfNotExist=true
    username: root
    password: "Ts171201@"
  data:
    redis:
      host: localhost
      port: 6379
      password: "123456"

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

profile:
  service:
    url: http://localhost:8087

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always